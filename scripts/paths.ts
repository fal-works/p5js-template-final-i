import type { Paths } from "@fal-works/ts-build-helper";

export const paths: Paths = {
  dirs: {
    src: "src",
    tsOut: "ts-out",
    dist: "dist",
  },
  filenames: {
    srcEntry: "main.ts",
    dist: "script.js",
  },
};
